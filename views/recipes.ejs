<%- include('bootheader') -%>

    <form method="post" action="/processRecipe">
      Name: <input type="text" name="name">
      <br>
      <br>
      Ingredient: <input type="text" name="ingredient">
      <br>
      <br>
      Description: <input type="text" name="description">
      <br>
      <br>
      <input type="submit" value="submit" class="btn btn-primary">
    </form>

    <form method="post" action="/recipeDelete">
      <ol>
        <% for(i = 0; i<recipes.length; i++){  %>
            <li>
                  <b> Name:<%= recipes[i].name %> </b>
                      user: <%= recipes[i].userName %>   <br>
                      Created At:<%= recipes[i].createdAt %>   <br>
                      ingredient:<%= recipes[i].ingredient %>    <br>
                  <i> description:<%= recipes[i].description %>    </i>
                  <% if (recipes[i].userName.equals(user.Name)) { %>
                      <input type="checkbox" name="delete" value="<%= recipes[i]._id %>">
                  <% } %>
            </li>
        <% } %>
        </ol>
        <input type="submit" value="delete marked">
      </form>

<%- include('bootfooter') -%>
